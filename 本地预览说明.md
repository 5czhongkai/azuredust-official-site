# 本地预览说明

## 离线字体

当前页面字体已经改为纯本地字体栈，不再依赖 Google Fonts 或任何在线字体资源。

字体策略：

- 标题：优先使用本机已有的高端衬线字体，如 `Iowan Old Style`、`Baskerville`、`Songti SC`
- 正文与导航：优先使用本机已有的现代无衬线字体，如 `Avenir Next`、`SF Pro Text`、`PingFang SC`

这意味着在完全离线环境下，页面仍可稳定显示，不会因为外部字体请求失败而回退到不可控状态。

## 本地预览方式

项目已内置一个零依赖预览脚本：

- 脚本路径：`scripts/preview.py`
- 默认地址：`http://127.0.0.1:4173`

在项目根目录运行：

```bash
python3 scripts/preview.py
```

如需自定义端口：

```bash
python3 scripts/preview.py --port 8080
```

## 适用场景

- 本地静态页面开发
- 微信打开前的本机预览
- 不依赖 Node.js 的轻量调试
- 完全离线环境下的页面查看
